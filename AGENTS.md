# Zenstudy 教材制作の継続指示

## このプロジェクトの目的

- このプロジェクトとチャットは、Zenstudy のライブ授業教材の制作専用とする。
- 利用者は、制作に関する依頼・方針・会話・成果物・変更履歴を、このプロジェクトと GitHub の `koushinjyuku-material/zen_live` に残すことを希望している。
- 「Zenstudy に記録する」は、このローカルの教材制作プロジェクトを指す。ZEN Study のサービス内への投稿・登録は依頼の対象に含まれない。
- 利用者が毎回同じ指示を繰り返さなくてよいよう、確定した条件や好みをファイルに保存して次回の作業に使う。新しい明示的な指示があれば優先し、変更理由を記録する。
- 利用者とのやり取りと制作記録は原則として日本語。日付は日本時間（Asia/Tokyo）を使う。

## 作業開始時

1. `README.md`、`docs/制作方針.md`、`docs/進行状況.md` と、`records/` の最新の日次記録を読む。
2. 対象となる教材の既存ファイル、関連する過去の決定を確認する。
3. Git のブランチ、接続先、未コミットの変更を確認し、既存の作業を保全する。
4. すでに記録されている条件を聞き直さず、未確定の条件だけ必要な段階で確認する。推測は確定事項と区別する。

## 教材の保存

- 教材は `materials/` の授業・単元別フォルダーに保存する。編集元、配布用ファイル、図版、参照情報を関連付ける。
- 教科、対象学年、授業時間、出力形式などは、利用者の指定や既存教材から確認する。未指定の条件を固定ルールとして創作しない。
- 採用した内容、修正理由、出典、検証結果を記録する。生成した教材は内容の整合性を確認し、PDF・スライド等は必要に応じて表示も確認する。
- 外部ページや一時的な出力先だけに成果物を置かず、保存できる成果物はこのプロジェクトにも残す。

## 各応答・作業終了時の記録

1. `records/YYYY-MM-DD.md` に、この作業で確認できた新しい利用者の依頼・補足と最終回答を原文として追記する。制作方針に関わる途中の説明も残す。原文と要約を明記し、読めない過去の会話を再現したふりをしない。
2. 同じ記録に、実施内容、変更ファイル、決定と理由、確認結果、未解決事項、GitHub 反映結果を残す。ツール実行は結果と必要な参照先を記録する。
3. 継続して適用する条件は `docs/制作方針.md` に反映する。現在地・次の作業・未反映の変更は `docs/進行状況.md` に反映する。
4. 自分が今回変更した対象ファイルを確認し、意味のある単位で GitHub にコミットする。利用可能なら Git の明示的なステージ・コミット・push を使う。下記の認証状態ではアプリの GitHub 連携を使い、最新のブランチを親にして変更対象だけをコミットする。この通常の記録・同期は利用者の継続的な依頼に含まれるため、毎回実行許可を聞き直さない。実行環境の権限要求がある場合はそれに従う。
5. GitHub の書き込み結果とリモートのコミット・内容を確認してから、GitHub 保存済みと報告する。未接続・権限不足・競合などで未反映なら、ローカルの記録を保ち、未反映であることと次の対応を明記する。

## 認証と同期方法（2026-09-09 確認）

- 端末の Git/GitHub CLI は `naitonori` として認証されており、対象リポジトリーへの push は 403 で失敗した。認証の変更が確認できるまでは、同じ push を繰り返さず、書き込み権限のあるアプリの GitHub 連携を使う。
- GitHub 連携では対象ブランチの最新コミットとツリーを取得し、そのツリーを基に今回の変更を反映する。複数ファイルはツリーとコミットを作成して `force: false` でブランチを更新し、他の変更を保持する。
- 連携経由の保存後は、ローカルの作業を保全して GitHub の履歴を取り込み、ファイル内容とブランチの同期を確認する。ユーザーの端末全体の認証設定は勝手に変更しない。

## GitHub の扱い

- 保存先: `https://github.com/koushinjyuku-material/zen_live`
- 基本ブランチ: `main`。既存の作業ブランチやリポジトリー規則があれば尊重する。
- 認証情報、秘密鍵、アクセストークンは記録・コミットしない。他のプロジェクトの会話やファイルを混ぜない。
- 無関係な変更をコミットに含めず、force push、履歴の削除、リポジトリーの公開範囲変更を通常の同期として行わない。
- 最終回答では、成果物の場所と実際の保存状況を簡潔に伝える。記録のみの変更には不要なテストを追加しない。
